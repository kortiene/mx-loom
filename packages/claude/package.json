{
  "name": "@mx-loom/claude",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "mx-loom Claude Agent SDK binding. T111: the JSON Schema → Zod converter — turns each canonical `mx_*` descriptor's draft-07 `input_schema` into an equivalent Zod schema the SDK's `tool()` accepts. Fail-closed on any construct outside the supported subset; equivalence is proven against the registry's Ajv seam. (T110 builds the `tool()`/`createSdkMcpServer()` registration on top of this.)",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@mx-loom/registry": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8"
  }
}
